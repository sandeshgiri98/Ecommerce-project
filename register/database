Create Database Project;

CREATE TABLE register (
    id int NOT NULL AUTO_INCREMENT,
    name varchar(255),
    email varchar(50)NOT NULL,
    phone int(10),
    dob varchar(20),
    password varchar(50),
    PRIMARY KEY (id)
);